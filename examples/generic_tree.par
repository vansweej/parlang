type Tree a = Leaf a | Node (Tree a) (Tree a) in
let myTree = Node (Leaf 10) (Node (Leaf 20) (Leaf 30)) in
let sumTree = rec sumTree -> fun tree ->
    match tree with
    | Leaf value -> value
    | Node left right -> sumTree left + sumTree right
in
sumTree myTree
